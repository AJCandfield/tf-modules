name: Toolchain image

on:
  push:
    branches:
      - main
    paths:
      - ".mise.toml"
      - "Dockerfile"
      - ".dockerignore"
      - ".github/workflows/toolchain-image.yml"
  pull_request:
    paths:
      - ".mise.toml"
      - "Dockerfile"
      - ".dockerignore"
      - ".github/workflows/toolchain-image.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v6
        id: metadata
        with:
          images: ghcr.io/ajcandfield/tf-modules-ci
          tags: type=raw,value=build-${{ github.sha }}
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Promote image
        run: |
          source="ghcr.io/ajcandfield/tf-modules-ci:build-${GITHUB_SHA}"
          docker buildx imagetools create \
            --tag ghcr.io/ajcandfield/tf-modules-ci:latest \
            --tag ghcr.io/ajcandfield/tf-modules-ci:sha-${GITHUB_SHA::7} \
            "$source"
